#include <stdio.h>
int main()
{
  int numero=0;
  printf("Ingrese un numero\n");
  scanf("%d",&numero);

  if(numero==0)
  {
    printf("El numero ingrsado es cero\n");
  }
  else
  {
    if(numero1<0)
    {
      printf("El número ingresado es negativo");
    }
    else
    {
      printf("El número ingresado es positivo");
    }
  }
 return (0);
}
